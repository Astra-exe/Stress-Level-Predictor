export type StressFormData = {
  age: string,
  gender: string,
  height: string,
  weight: string,
  job: string,
  sleepHours: string,
  stepsByDay: string,
  heartRatio: string,
}

export type UserAgeGenderData = {
  age: string;
  gender: string;
};

export type UserWeightHeightData = {
  height: string;
  weight: string;
};

export type UserJobSleepData = {
  job: string;
  sleepHours: string;
};

export type UserStepsHeartData = {
  stepsByDay: string;
  heartRatio: string;
};